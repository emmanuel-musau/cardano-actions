packages:
  - "packages/*"
  - "apps/*"
  - "examples/*"

# Fail the install on the wrong Node/pnpm rather than at a confusing NodeNext
# resolution error later. Versions are pinned in package.json `engines`.
engineStrict: true
